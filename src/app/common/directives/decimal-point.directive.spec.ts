import { DecimalPointDirective } from './decimal-point.directive';

describe('DecimalPointDirective', () => {
  it('should create an instance', () => {
    const directive = new DecimalPointDirective();
    expect(directive).toBeTruthy();
  });
});
